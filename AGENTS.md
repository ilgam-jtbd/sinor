# AGENTS.md — 시놀 제품 개발 작업 규칙 (OpenAI Codex 등 코딩 에이전트용)

Codex는 이 파일을 자동으로 읽습니다. Claude Code는 `CLAUDE.md`를 읽습니다.
내용은 같고, 이 파일은 **명시적 지시 형식**으로 씁니다 — Codex는 탐색 여지보다 못 박힌 조건에서 결과가 좋습니다.

---

## 저장소 성격

분석·기획 전용. **시놀 실서비스 코드는 이 저장소에 없습니다.** 여기서 만드는 것은 문서·규격·티켓·프롬프트입니다.

## 작업 시작 전 읽을 파일 (순서대로)

1. `docs/03_정합기준_정본_2026-08.md` — 모든 수치의 단일 출처
2. `docs/02_제품개발_운영체계_v1.0.md` — 협업 규격
3. `docs/00_문서학습_지식베이스.md` — 전략·기획 요약

**정본에 없는 수치를 생성하지 마십시오.** 필요한 수치가 없으면 `[확인 필요]`로 표기하고 멈춥니다.

## 금지 사항 (위반 시 작업 무효)

- 추측을 사실처럼 서술하지 않는다. 근거를 함께 적는다
- 신뢰도 표기 없이 확정하지 않는다 (`확정 / 유력 / 추정 / 미확인`)
- 프로덕션 변경 금지 (배포·마이그레이션·설정 변경·계정 생성)
- 개인정보 조회·다운로드 금지 (스키마 구조만 확인)
- 요구사항을 임의로 확장하지 않는다
- 측정 이벤트를 별도 작업으로 미루지 않는다
- 막혔을 때 우회 구현하지 않는다 — 보고하고 멈춘다

## 작업 지시를 받을 때 확인할 것

지시에 아래가 없으면 **작업을 시작하지 말고 되물으십시오.**

- [ ] 정확한 파일 경로
- [ ] 입력(props·데이터 구조)과 출력(렌더 결과·반환값)
- [ ] 검증 가능한 완료 조건
- [ ] 부착할 이벤트 이름 (노출·탭·전환)
- [ ] 유지해야 할 기존 인터페이스

## 완료 조건 (모든 구현 작업 공통)

- [ ] 기능 동작
- [ ] 이벤트 부착 완료 (노출 · 탭 · 전환)
- [ ] 가드레일 지표 측정 가능
- [ ] 실기기 확인 (Android · iOS) — 앱 작업인 경우
- [ ] 이번에 하지 않은 것(제외 범위) 명시

## 시놀 고유 구현 제약 — 어길 경우 재작업

| 대상 | 규칙 | 위반 시 |
|---|---|---|
| 수익 카드 | 콘텐츠 카드와 동일 컴포넌트. 테두리색·배경색·「광고」 표기 금지 | 광고로 인지 → 스크롤 이탈 |
| 수익 카드 3종 | 연속 배치 금지. 콘텐츠 섹션 사이에 배치 | 광고 구간으로 인지 |
| 시럽인연 상담 / 여행 결제 페이지 | 앱 설치 유도 삽입 금지 | 전환 흐름 절단 (이탈 7.0% 페이지 훼손) |
| 자녀 타깃 페이지 | 설치 버튼 대신 공유 버튼(카카오·문자) | 설치 주체 불일치 — 계정 미사용 |
| 웹→앱 유도 | 시놀 앱 단일. 시럽 앱은 `/silove` 내부에서만 + 성인 등급 안내 | 사용자 혼란 · 측정 분산 |
| 텍스트·터치 | 본문 최소 17px · 터치 영역 최소 48px · 명도 대비 4.5:1 | 시니어 사용성 실패 |
| 안 읽음 표시 | 탭=점, 목록=숫자. 점 해제는 대화방 실제 열람 시점 | 알림 신뢰도 붕괴 |
| 가로 스크롤 카드 | 다음 카드가 약 20% 걸치게 배치 | 스크롤 시도 자체가 발생하지 않음 |
| 배너 자동 롤링 | 5초 이상 | 읽기 전에 넘어감 |
| 카테고리 아이콘 | 한글 라벨 병기 필수 (아이콘 단독 금지) | 인지율 저하 |
| 가격 표시 | 실제 상품 가격과 연동. 하드코딩 금지 | 가격 불일치 민원 |

## 프롬프트를 작성해 Codex에 넘길 때의 골격

```
파일: [정확한 경로]
작업: [한 문장]

입력: [props / 데이터 구조]
출력: [렌더 결과 / 반환값]

요구사항:
- [구체 항목]

제약:
- 기존 [X] 인터페이스 유지
- [Y] 라이브러리만 사용
- 위 "시놀 고유 구현 제약" 표 준수

완료 조건:
- [ ] [검증 가능한 조건]
- [ ] 이벤트 부착: [이벤트명]
```

## 산출물 위치

| 종류 | 경로 |
|---|---|
| 보고서 | `docs/reports/시놀_<주제>_YYYYMMDD.md` |
| One-pager | `docs/one-pagers/<기능명>.md` |
| 프롬프트 | `docs/prompts/` |

## 역할 경계

이 에이전트는 **구현과 조사**를 담당합니다. 전략 판단·우선순위 변경은 하지 않습니다.
불가능하거나 더 나은 방법이 보이면 **실행하지 말고 제안으로 기록**하십시오.

## 운영 지시 — 토큰·컨텍스트·캐시 (2026-09-03)

전문: `.claude/skills/ops-token-cache/SKILL.md` (Claude Code용 Skill이지만 원칙은 동일하게 적용).

- 우선순위: 정확성 > 안전성 > 기존 의사결정 준수 > 작업 범위 준수 > 검증 > 속도 > 토큰 절감
- 요청 범위를 먼저 확정하고 관련 파일만 탐색한다. 전체 스캔·전체 로그·전체 테스트로 시작하지 않는다
- 큰 로그는 필터링(`grep | head`)해서 넣는다
- URL·DB·가격·핵심 CTA·승인된 Copy·robots·noindex·대규모 디자인은 `[승인 필요]`로 보고하고 멈춘다
- 완료 보고는 `[완료] 변경/검증/결과/미해결/다음 작업` 형식으로 짧게. 전체 diff·로그를 복사하지 않는다
