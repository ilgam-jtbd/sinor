# 티켓 작성 예시 — 측정 선행 과제

> **작성 예시입니다.** 빈 양식은 `docs/templates/linear-ticket.md`.
> Linear 팀 확정 후 이 내용을 그대로 이슈로 올립니다.

---

**제목**
`[MEASURE] GA4 search_term 수집 + 결과 0건 로그 — 홈 검색바 개발 선행 (수집 확인까지)`

**라벨** `MEASURE` `APP:HOME`
**우선순위** Urgent — 후속 티켓 전부의 선행 조건

---

## 배경

One-pager: `docs/examples/one-pager_앱홈-모임-검색바.md`

28일간 4,232명이 5,683회 검색(GA4 2026.07.09~08.05)하는데 검색어가 수집되지 않는 것으로 추정됩니다. 네이버 검색광고는 등록 키워드 3,655개 중 클릭 발생이 48개뿐인데, **회원이 실제로 치는 검색어를 모르기 때문에** 교체할 근거가 없습니다. 이 티켓은 화면 개발이 아니라 **판정 가능 상태를 만드는 일**입니다.

## 요구사항

- [ ] 회원이 검색을 실행하면 `view_search_results` 이벤트에 `search_term` 파라미터가 함께 전송된다
- [ ] 검색 결과가 0건인 경우 별도 로그로 적재된다 (존재하지 않는 모임에 대한 수요 신호)
- [ ] 운영자가 월 1회 상위 검색어와 0건 검색어를 조회할 수 있다
- [ ] 홈 각 섹션의 노출·탭 이벤트가 섹션별로 구분되어 수집된다

## 완료 조건 (Definition of Done)

- [ ] GA4 실시간 보고서에서 `search_term` 값이 확인된다 (테스트 검색 5건)
- [ ] 이벤트 부착: `view_search_results`(search_term) · `search_no_result`(search_term) · `section_impression`(section_id) · `section_tap`(section_id)
- [ ] 가드레일 지표 측정 가능: 앱 실행 후 첫 액션까지 시간 · 모임 참여 후 이탈률
- [ ] 개인정보가 검색어로 유입되는 경우의 처리 방침 확인 (전화번호·이름 패턴)
- [ ] 실기기 확인 (Android · iOS)

## 제외 범위

- 검색어 대시보드 UI 구축 — 이번에는 조회 가능하기만 하면 됩니다
- 자동완성·오타 교정
- 검색 품질 개선 (랭킹·형태소)
- 네이버 광고 키워드 실제 교체 — 데이터가 4주 쌓인 뒤 별도 티켓

## 의존

- **선행 없음. 즉시 착수 가능** (종합전략 v3 90일 과제 「전환추적 설치 + 서치콘솔 연결」과 동일 구간)
- 후행: 홈 검색바 개발 · 홈 섹션 개편 · 네이버 광고 키워드 교체
- 확인 필요: 앱 내 검색 구현 방식(DB LIKE vs 검색엔진) — 검색어 로그 적재 지점이 달라집니다

## 에이전트 프롬프트 (Claude Code)

```markdown
## 역할
시놀 앱의 측정 체계를 세우는 엔지니어.

## 목표
홈 검색바 개편의 효과를 판정 가능하게 만든다. 지금은 개편해도 좋아졌는지 알 방법이 없다.

## 근거
28일간 검색 4,232명·5,683회(GA4 2026.07.09~08.05)인데 search_term 미수집 추정.
네이버 광고 키워드 3,655개 중 클릭 48개 — 교체 근거가 없는 상태.
정합기준: docs/03_정합기준_정본_2026-08.md

## 작업
1. [탐색] 현재 검색 구현 위치와 GA4 이벤트 부착 지점을 먼저 파악한다. 이미 수집 중인지 확인한다
2. [구현] view_search_results 에 search_term 파라미터 추가, 0건 검색어 별도 로그 적재,
   홈 섹션별 노출·탭 이벤트 부착
3. [검증] 테스트 검색 5건이 GA4 실시간 보고서에 나타나는지 확인

## 제약
- 프로덕션 변경 금지 / 개인정보 접근 금지
- 검색어에 개인정보(전화번호·이름)가 섞일 수 있다 — 마스킹 방침을 확인하고 진행한다
- 기존 검색 동작을 깨지 않는다
- 불가능하면 대안을 제시하고 멈춘다

## 완료 조건
- [ ] GA4 실시간에서 search_term 확인
- [ ] 이벤트 부착: view_search_results / search_no_result / section_impression / section_tap
- [ ] 가드레일 2종 측정 가능

## 출력
변경 파일 목록 + 이벤트 명세표(이벤트명·파라미터·발생 지점) + 미확인 항목과 확인 방법
```
