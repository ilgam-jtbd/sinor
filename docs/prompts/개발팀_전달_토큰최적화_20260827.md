# 개발팀(우종선) 전달 — Claude Code·Codex 토큰 최적화 가이드 (2026-08-27)

> CC 검증·배포 완료. 아래 "전달 메시지"를 그대로 개발팀에 보내면 됩니다.

---

## 전달 메시지 (복사해서 우종선님께)

```
안녕하세요 우종선님. 개발팀에서 나온 "Claude Code·Codex 토큰이 빨리 닳는다"
문제에 대한 진단·처방 가이드를 사내 페이지로 정리해 올렸습니다.

▶ 가이드(내부용·검색 비공개) : https://ilgam-jtbd.github.io/sinor/devops/

무엇인가 —
- "아껴 쓰자"는 당부가 아니라 진단 → 처방 → 측정 순서를 가진 실행 문서입니다.
- 원인은 대개 셋: 긴 명령 출력, 통째로 읽은 큰 파일, 한 세션을 오래 붙잡기.
- 개발자용이라 터미널 블록마다 복사 버튼이 있어 /context·/compact·AGENTS.md
  골격을 바로 복사해 쓸 수 있습니다.

오늘 30분 안에 —
1) /context 로 지금 무엇이 자리를 먹는지 본다
2) 진단 프롬프트(수정 없음)로 원인부터 좁힌다
3) /autocompact 500k 를 한 번 걸어둔다
한 번에 하나만 바꾸고 같은 작업으로 다시 재는 것 — 이 순서만 지켜도 됩니다.

Codex 병행 팀에게 특히 —
Codex는 CLAUDE.md 대신 AGENTS.md를 읽습니다. 토큰이 빨리 닳는 가장 흔한 이유는
배경 설명을 프롬프트에 매번 길게 붙이는 것입니다. 반복 배경은 AGENTS.md에 한 번만
적고 프롬프트엔 이번 할 일만 씁니다.

이 저장소 현황(CC가 미리 감사) —
- 루트 CLAUDE.md 109줄 (기준 200줄 이하 → 이미 양호, 분리 급하지 않음)
- AGENTS.md 이미 존재
- 다만 두 파일이 "내용 동일" 미러 방식입니다. 가이드는 한쪽을 정본(SSOT)으로
  두고 다른 쪽은 포인터만 두는 방식을 권합니다(중복=드리프트 위험). 어느 방식을
  갈지는 팀 판단입니다 — 바꾼다면 PR로만, 자동 병합은 하지 않습니다.

검증 결과 — 콘솔 오류 0 · 데스크톱 가로 스크롤 없음 · 복사 버튼 정상.
(모바일 390px에서 표 영역이 약간 넘쳐 가로 스크롤 27px 발생 — 읽기엔 지장
없으나 원하시면 한 줄 CSS로 잡겠습니다.)

문의는 저(PO)한테 편하게 주세요.
```

---

## CC 세션 붙여넣기 프롬프트 (개발자가 CC로 §6 1~3단계 직접 할 때)

```
sinor 저장소에서 Claude Code·Codex 토큰 최적화 §6 1~3단계를 점검해 주십시오.
변경은 PR로만 올리고 병합은 사람이 합니다. 자동 병합 금지.

1) 루트 CLAUDE.md 줄 수 감사 — 200줄 초과 여부(현재 109줄). 초과 시에만 분류.
2) .claude/rules/ 또는 하위 폴더 CLAUDE.md로 조건부 로딩 분리 제안(선택).
3) AGENTS.md(이미 존재)와 CLAUDE.md의 중복 여부 점검 —
   SSOT 하나 + 포인터로 정리할지 제안. 지금은 두 파일 내용 동일(미러).

회신: 감사 결과 · 변경 제안(있으면 PR) · 병합은 보류(사람 검토).
```

---

## 검증 기록 (CC)

| 항목 | 결과 |
|---|---|
| 자체완결(외부 리소스) | ✅ 0개 (인라인 CSS/JS만) |
| `noindex` | ✅ 적용 |
| 콘솔 오류 | ✅ 0건 |
| 가로 스크롤(데스크톱 1000px) | ✅ 없음 |
| 가로 스크롤(모바일 390px) | ⚠️ 27px 넘침 — 표 영역. 읽기 지장 없음, 한 줄 CSS로 수정 가능(코워크 '수정 금지' 방침이라 보류·문의) |
| 복사 버튼(navigator.clipboard) | ✅ 동작('복사됨' 표시). 주석(`#`) 줄도 함께 복사됨 — 터미널 맥락 유지라 문제 없음 |

## §6 1~3단계 착수 판단 (CC)
- **1단계(CLAUDE.md ≤200)**: **이미 충족**(109줄). 조치 불필요.
- **2단계(규칙 분리)**: **선택** — 109줄이면 급하지 않음. 향후 늘어나면 `.claude/rules/`로.
- **3단계(AGENTS.md 신설)**: **이미 존재.** 단, CLAUDE.md와 "내용 동일" 미러 → 가이드 권장(SSOT+포인터)과 다름. **전환은 governance 판단이라 자동 진행하지 않고 PR + 사람 검토로.** CLAUDE.md §9(상대 영역 임의 결정 금지) 준수.

---

*작성 2026-08-27 · CC · 내부용 · 근거: 코워크 전달 문서 · HTML 실측 검증 · CLAUDE.md/AGENTS.md 감사*
