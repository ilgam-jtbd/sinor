# 코워크 요청 — OpenAI Codex 공식 문서사이트 2건 확인 (2026-08-27)

> **왜 CC가 못 하나** — `developers.openai.com/codex`는 이 세션의 **org 이그레스 정책상 403 차단**입니다. 프록시 README가 *"우회하지 말고 차단 호스트를 보고하라"* 고 명시(CLAUDE.md §8 동일)하므로, **우회하지 않고** 브라우저가 되는 Cowork에 넘깁니다.
> **이미 확정된 것** — Codex 동작·값은 **오픈소스 저장소**(`github.com/openai/codex`)와 **agents.md 스펙 원본**(GitHub raw)으로 전부 확인 완료(`docs/reports/공식출처_분석_토큰컨텍스트_20260827.md`). **남은 건 문서사이트 '문구' 인용 2건뿐**입니다.

> **⚠️ 2026-08-29 갱신 — 문서 홈 URL 변경.** 공식 문서가 `developers.openai.com/codex` → **`learn.chatgpt.com`** 으로 이전. `llms.txt` 인덱스가 있고 페이지 URL 뒤에 `.md`를 붙이면 마크다운 원문이 나옵니다.
> **#2(컨텍스트 관리 페이지)는 해소됨** — 사용자가 「Slash commands」 페이지 원문을 제공해 `/compact`·`/status`·`/init`(AGENTS.md 스캐폴드)·`/mcp`·`/side` 공식확인 완료. **잔여는 #1 한 건.**

## 확인해 줄 것 (잔여 1건)

| # | 페이지 | 확인할 문구 | 지금 우리 표기 |
|---|---|---|---|
| 1 | **`https://learn.chatgpt.com/docs/agent-configuration/agents-md.md`** ("Custom instructions with AGENTS.md" — llms.txt 인덱스로 정확 URL 확보, 2026-08-29) | ① **AGENTS.md 권장 길이/크기**에 대한 공식 서술이 있는가 (코드상 하드 상한은 `project_doc_max_bytes = 32768` = 32KB로 확인됨 — 문서에 별도 권장이 있나) ② **"규칙을 여러 파일에 중복하지 말라"** 류의 공식 권고가 있는가 | 학습 HTML: "AGENTS.md 32KB 상한(코드 확인)" · "규칙 중복 금지"는 **시놀 팀 권고**로만 표기 |

> 참고 — 같은 인덱스에서 함께 열람하면 좋은 페이지(선택): `docs/agent-configuration/subagents.md`(Codex 서브에이전트) · `docs/hooks.md`(Codex 훅) · `docs/extend/mcp.md`(MCP). 페이지 존재는 확인됐고 본문만 미열람 상태입니다.

## 회신 형식

각 항목에 대해:
- **원문 인용**(1~2문장) + **정확한 URL**
- 없으면 "문서에 해당 서술 없음"
- (있으면) CC가 `공식출처_분석` 문서의 해당 "⚠️ 미확인 → Cowork 확인 대기" 행을 **공식확인으로 승격**하고, 필요 시 학습 HTML 문구를 공식 인용으로 교체합니다.

## 하지 말 것
- 문서에 없는 내용을 만들어 넣지 않습니다(빈칸이 낫습니다).
- 학습 HTML의 **디자인·구조는 그대로** — 문구만 공식 인용으로.

---

*작성 2026-08-27 · CC→Cowork · 내부용 · 사유: developers.openai.com org 이그레스 403(우회 금지·보고 원칙)*
