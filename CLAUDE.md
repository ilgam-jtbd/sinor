# CLAUDE.md — 시놀 제품 개발 작업 규칙 (Claude Code용)

이 파일은 Claude Code가 이 저장소에서 작업할 때 자동으로 읽습니다.
Codex를 쓰는 경우 `AGENTS.md`를 읽으십시오. 두 파일의 내용은 같고 형식만 다릅니다.

---

## 0. 이 저장소의 성격

**분석·기획 전용 저장소입니다. 시놀 실서비스 코드는 여기 없습니다.**

- 여기서 하는 일: 진단·기획 문서, 화면 목업 규격, 티켓·프롬프트 산출, 기술 실사 보고서
- 여기서 하지 않는 일: 실서비스 기능 구현
- 실서비스 코드 작업이 필요하면 해당 저장소를 `add_repo`로 세션에 추가한 뒤 진행합니다

## 1. 작업 전 반드시 읽을 것

| 순서 | 파일 | 왜 |
|---|---|---|
| 1 | `docs/03_정합기준_정본_2026-08.md` | **모든 수치의 단일 출처.** 다른 숫자를 쓰지 않습니다 |
| 2 | `docs/02_제품개발_운영체계_v1.0.md` | 3자 협업 규격 · 문서 파이프라인 |
| 3 | `docs/00_문서학습_지식베이스.md` | 전략 v3 · 기획서 · 개발요구서 요약 |

수치가 기억과 다르면 **정합기준 정본이 맞습니다.** 정본에 없는 수치를 새로 만들지 마십시오.

## 2. 판단 기준 — 여기서 갈리면 이 순서로 되돌아옵니다

PO 교육자료 W1~W4에서 가져온 기준입니다. 우선순위·범위 논쟁이 생기면 이 줄들로 판정합니다.

1. **좋은 문제 = 겪는 사람 수 × 고통의 크기 ÷ 필요한 리소스.** 목표는 단위시간당 임팩트 최대화
2. **지표는 쪼갠다.** `매출 = 결제고객 × 객단가` · `MAU = f(신규, 리텐션, 부활)` — 쪼개기 전에는 무엇을 올릴지 정할 수 없습니다
3. **레버는 "전환율이 높은데 비중이 낮은 곳"을 먼저 키운다**
4. **최초 가설의 90% 이상은 틀린다.** 그래서 작게·빠르게·측정 가능하게
5. **A/B 테스트는 필수.** 불가능하면 왜 불가능한지와 대체 판정 방법을 적습니다
6. **요구사항을 그대로 만들지 않는다.** 기저 문제를 이해하고 최소 리소스 해법을 제시합니다
7. **생애주기별로 운영 방식이 다르다.** 시럽인연=Maturity(실행 퀄리티) · 시놀 커뮤니티=Growth 종료(비용·자동화) · 시니어 일자리=Introduction(개발 없는 검증). **같은 방식으로 다루지 마십시오**

## 3. 절대 규칙

1. **추측을 사실로 쓰지 않는다.** 모든 확정 항목에 근거(파일 경로·명령어·응답 헤더·캡처)를 붙입니다
2. **신뢰도를 표기한다** — `확정 / 유력 / 추정 / 미확인` 4단계
3. **프로덕션을 변경하지 않는다.** 조사는 읽기 전용. 배포·마이그레이션·설정 변경·계정 생성 금지
4. **개인정보에 접근하지 않는다.** 회원 데이터 조회·다운로드 금지. 스키마 구조만 확인
5. **부하를 주지 않는다.** 크롤링은 요청 간 1초 이상, 총 200요청 이내
6. **요구사항을 임의로 확장하지 않는다.** 필요하면 티켓에 제안으로 남깁니다
7. **측정 이벤트를 나중으로 미루지 않는다.** 같은 작업에 포함합니다 — 나중에 붙이겠다는 항목은 대부분 붙지 않습니다
8. **막히면 우회하지 말고 보고한다.** 임의 대안을 실행하지 않습니다
9. **미확인 항목은 비워두지 않는다.** "미확인 — 확인 방법"을 적습니다

## 4. 새 기능 작업의 진입 조건

```
① 진단(실측) → ② One-pager → ③ 목업 → ④ 디자인 → ⑤ 티켓 → ⑥ 에이전트 프롬프트 → ⑦ 검증 → ⑧ 이터레이션
```

**②를 건너뛰고 ⑤로 가지 마십시오.** 개발은 되는데 왜 만들었는지 아무도 모르는 상태가 됩니다.

- One-pager 양식: `docs/templates/one-pager.md` · 작성 예시: `docs/examples/`
- **가드레일 지표가 비어 있으면 반려합니다.** 성공지표만 있는 기획은 "발견은 늘었는데 경험은 나빠진" 상태를 성공으로 읽습니다

## 5. 완료 조건 (Definition of Done)

어떤 구현 작업이든 아래를 만족해야 완료입니다.

- [ ] 기능 동작
- [ ] **이벤트 부착 완료** (노출 · 탭 · 전환)
- [ ] **가드레일 지표 측정 가능**
- [ ] 실기기 확인 (Android · iOS) — 앱 작업인 경우
- [ ] 제외 범위 명시 (이번에 하지 않은 것)

## 6. 시놀 고유 제약 — 자주 틀리는 곳

| 항목 | 규칙 |
|---|---|
| 수익 카드 | 콘텐츠 카드와 **동일 컴포넌트** 사용. 테두리색·배경색·「광고」 표기 금지 — 광고로 읽히면 스크롤 이탈 |
| 수익 카드 배치 | 3종(단체미팅·인연 진단·이번 주 여행)을 **연속 배치 금지**. 콘텐츠 사이에 끼웁니다 |
| 앱 유도 | 페이지군마다 목표는 **하나**. 시럽인연 상담(이탈 7.0%)·여행 결제 페이지에는 **앱 유도 금지** |
| 자녀 타깃 | 설치 버튼이 아니라 **공유 버튼**. 읽는 사람은 4050 자녀, 쓸 사람은 60대 부모 |
| 웹→앱 유도 | **시놀 앱 단일**. 시럽 앱 유도는 `/silove` 내부에서만 + **만 19세 이상(19+) 안내 병기** (App Store 실물 등급 19+·'성적인 내용/노출' 표시 — 2026-08-27 확인) |
| 접근성 | 본문 최소 17px · 터치 영역 최소 48px · 명도 대비 4.5:1 |
| 안 읽음 표시 | 탭 레벨은 점(dot), 목록 레벨은 숫자. 점 해제는 **대화방 실제 열람 시점** |
| 결제 | 무통장 입금 기본값은 정기결제를 원천 차단합니다. 카드 전환이 구독의 전제 |

## 7. 산출물 위치 규칙

| 종류 | 경로 |
|---|---|
| 기술 실사·분석 보고서 | `docs/reports/시놀_<주제>_YYYYMMDD.md` |
| One-pager | `docs/one-pagers/<기능명>.md` |
| 에이전트 프롬프트 | `docs/prompts/` |
| 템플릿 | `docs/templates/` (수정 시 예시도 함께 갱신) |

## 8. Git

- 개발 브랜치: `claude/learning-session-b0yeka`
- 커밋 메시지는 한국어로, **무엇을 왜 바꿨는지** 한 줄 요약 + 상세
- 대외비 원본(pptx·docx·pdf)은 커밋하지 않습니다. 요약본만 둡니다

## 9. 세션 간 협업

| 주체 | 담당 | 하지 않는 것 |
|---|---|---|
| Cowork(시놀) | 전략 판단 · 문서 최종본 · 이해관계자 커뮤니케이션 | 코드 작성 |
| **Claude Code (여기)** | 기술 조사 · 구현 · QA · 배포 | **전략 판단, 우선순위 변경** |
| 전략기획 세션 | 진단 · 지표 설계 · 기획서·목업 초안 | 최종 승인 |
| 디자이너 | 목업 → 화면 · 컴포넌트 정의 | 기획 합의 없는 구조 변경 |

**어느 쪽도 상대 영역에서 임의 결정하지 않습니다.** 막히면 기록으로 남기고 넘깁니다.
구현이 불가능하면 → 제약을 보고 → Cowork가 대안 결정 → 기획서 갱신. 이 순서를 건너뛰지 마십시오.

## 10. 운영 지시 — 토큰·컨텍스트·캐시 (2026-09-03 은실장 지시)

전문은 `.claude/skills/ops-token-cache/SKILL.md`(33항)입니다. 여기에는 판단 기준만 둡니다.

- **우선순위**: 정확성 > 안전성 > 기존 의사결정 준수 > 작업 범위 준수 > 검증 > 속도 > 토큰 절감. 절감을 이유로 분석·검증을 생략하지 않습니다
- **범위 먼저**: 요청 확인 → 범위 확정 → 관련 파일 최소 탐색 → Decision 확인 → 실행 → 관련 범위만 검증. 전체 스캔·전체 로그·전체 테스트로 시작하지 않습니다
- **캐시 보호**: 세션 중 모델·effort·MCP 구성 변경 금지. 큰 로그는 `grep | head`로 걸러서 넣습니다
- **세션 전환**: 같은 작업 → 유지(커지면 `/compact`) · 다른 작업 → `/clear` · 방향 오류 → `/rewind`
- **승인 필요 변경**: URL·DB·가격·핵심 CTA·승인된 Copy·robots·noindex·대규모 디자인은 `[승인 필요]`로 보고하고 멈춥니다
- **완료 보고**: `[완료] 변경/검증/결과/미해결/다음 작업` 형식, 전체 diff·로그 미복사
- 이 저장소 메모: 앱 코드가 없어 codebase-overview는 해당 없음 — 문서 지도는 `docs/12_자산대장`·`/hub/`
